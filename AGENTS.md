# 项目执行入口

- 产品行为以 `需求方案.md` v2.6 为准；执行流程见 `docs/agents/development-workflow.md`。
- 恢复工作先读 `docs/agents/workflow-state.md`，沿用其中产物、待决项和证据边界。
- 初始计划只沿用 `public/data/study-plan.seed.json`，不另造示例或内容基线。
- 需求冻结、模型推演通过与应用运行验收是不同状态，不互相替代。
