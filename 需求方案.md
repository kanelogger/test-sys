# 系统分析师学习助手：需求文档与技术方案

> 基线版本：v1.9，2026-09-06。本文是唯一开发需求与技术规则来源；不再拆出重复的 docs 文档。运行数据以数据库为准；种子数据以版本化 manifest 为准。

## 一、产品目标与首版范围

做一个个人备考 Web 应用，打开后直接回答三个问题：**今天学什么、从哪里开始、做到什么程度算完成。**

已确认的初始状态：

- 已报名、已缴费，所有学习进度从零开始。
- 以 2026 年 10 月 24 日倒排，实际考试日期可修改。
- 默认周一至周四每天 90 分钟、周五 60 分钟、周末每天 240 分钟；支持单日调整。
- 首版仅提供站内提醒，关闭网页后不会通知。
- 面向本人使用，支持手机和电脑登录同步。

首版包含每日规划、学习记录、进度复盘、资料入口和计划调整。暂不开发完整题库、视频播放、自动论文评分、聊天机器人、阶段路线（阶段目标）、邮件或浏览器推送。

应用上线后的学习数据以数据库为准；现有 Markdown 作为初始计划和资料来源，不进行双向同步。

## 二、需求文档

### 1. 页面与核心流程

| 页面 | 核心内容与操作 |
| --- | --- |
| 今日学习 | 剩余备考天数、今日预算、按顺序排列的任务；开始、记录、完成、调整今日时间 |
| 学习计划 | 周视图、每日负荷、待重新安排任务；预览并应用重排 |
| 学习记录 | 查看和修改实际时长、学习产出、真题成绩、掌握情况 |
| 进度复盘 | 三科投入、任务完成率、完整卷正确率、案例主题覆盖、论文产出、薄弱项 |
| 资料库 | 按科目、主题和类型检索视频、网页、PDF；关联学习任务 |
| 设置 | 考试日期、开始日期、每周时间、单日例外、提醒时间、数据导出 |

手机端采用“今日、计划、记录、更多”底部导航；桌面端采用侧边导航。默认进入今日学习，不以图表作为首页主体。

首次使用：确认日期与时间预算 → 查看从零开始的计划预览 → 启用计划 → 进入第一个任务。开始日默认首次启用当天；当前备考目标日期默认2026-10-24，不以文档创建日期强制补过去的任务。

### 2. 每日任务

每个任务必须包含：

- 科目、主题和具体行动。
- 预计投入分钟数。
- 关联资料与定位信息；未核对的 PDF 页码留空。
- 完成标准，例如“闭卷解释三种开发模型并写出差异”。
- 必做或选做标记，以及安排原因。
- 本次应完成范围：可拆任务按计划项给出本次承诺的具体单位（视频起止秒、题号或检查项 ID），页面据此回答“做到哪里算完成”。

任务分为视频、知识回忆、选择题、案例、论文、复盘和复习。可拆任务按第6.5节分块；20分钟周复盘和不足25分钟的最终尾块是明确例外，完整限时训练保留连续时段。

不以“打开链接”“计时结束”自动判定完成。用户提交实际完成量后确认完成；未完成部分保留剩余工作量。

### 3. 学习记录

支持计时和手动补录。计时以开始、暂停、结束时间计算，刷新页面可恢复；忘记停止时允许修正。

不同任务记录不同成果：

| 类型 | 记录内容 |
| --- | --- |
| 视频/知识 | 已学范围、简短回忆、掌握程度 |
| 选择题 | 年份、实际题数、正确数、用时、完整性、是否分段及是否看过答案 |
| 案例 | 主题、独立作答内容或附件、遗漏得分点、待重答项 |
| 论文 | 题目、提纲或全文、限时情况、修改状态 |
| 通用 | 实际学习分钟、笔记、下一步 |

未做题时正确率显示“暂无数据”。缺题卷、分段卷、已看解析的卷分别标记，不进入“最近三套完整独立限时卷”的达标统计。

### 4. 从零开始的规划规则

采用第六节的确定性排程规范。开始日重新确认，零进度不继承已过日期的任务。首次诊断由 priority_class=2 保证占用首个连续容量足够的周末时段；论文素材由 priority_class=1、deadline_offset_days=6 保证首周排完；无法满足时显式报告缺口，不依赖排序巧合。

排程输入（考试日、开始日、每周时间段、单日例外、目标量）的修改不直接落库：作为待生效变更提交预览，预览列出设置差异以及移动、移除和无法安排的任务及分钟数；用户确认后在同一事务内原子保存变更与新计划，放弃预览则设置与日历均保持原值，不产生“设置已改、计划照旧”的长期不一致。提醒时间等不影响排程的设置直接保存；首次初始化尚无计划时设置直接确认保存。已完成记录不随重排移动，运行中或暂停中的任务固定，不参加自动重排。

### 5. 站内提醒

默认每天 20:00 为学习提醒时间，可配置。

- 打开应用时提示今日未完成任务、到期复习和待处理的计划缺口。
- 页面保持打开时，到提醒时间显示站内提示。
- 已完成任务不再提醒；同一提醒当日可暂时关闭。
- 返回页面、跨日和恢复窗口焦点时重新计算提醒状态。
- 不引入后台定时发送服务；提醒通过数据库状态和当前时间计算。

## 三、技术方案

### 1. 架构

```text
React / Vite / shadcn-ui / Tailwind
               │ HTTPS + 用户 JWT
               ▼
Node.js / Fastify / TypeBox
      ├─ 任务与记录接口
      ├─ 确定性排程与重排预览
      └─ 统计与资料访问
               │ 用户身份上下文
               ▼
Supabase Auth / PostgreSQL / 私有 Storage
```

采用 TypeScript，代码组织为：

- `apps/web`：前端页面、表单、请求与交互。
- `apps/api`：Fastify 服务、排程逻辑及业务接口。
- `packages/contracts`：共享 TypeBox 请求、响应定义。

前端使用 React Router、TanStack Query；表单以原生控件和 shadcn-ui 组件实现。TypeBox 定义作为接口契约来源，接入 Fastify Type Provider 与 OpenAPI，避免前后端分别维护类型。[Fastify 官方说明](https://fastify.dev/docs/latest/Reference/Type-Providers/)

部署默认使用单个 Node 容器：Fastify 提供 API 和 Vite 构建产物，保持同源访问；数据库、认证和附件由 Supabase 托管。

### 2. 数据设计

| 数据实体 | 职责 |
| --- | --- |
| `study_settings` | 用户、考试日、开始日、每周时间段、提醒时间（固定中国时区） |
| `day_overrides` | 单日可用分钟数覆盖 |
| `resources` | 资料类型、科目、主题、URL/私有文件位置、版本与定位信息 |
| `task_templates` | 初始任务、预计工作量、完成标准、依赖、优先级 |
| `tasks` | 用户任务、单位总量、派生进度、完成标准、关联资源、来源复习项（`source_review_item_id`） |
| `plan_versions` / `plan_items` | 排程版本（含生成输入快照）、任务日期、起止时间、当日顺序、承诺单位范围、分配分钟、所属组尝试（`schedule_group_attempt_id`，可空）及安排原因 |
| `schedule_group_attempts` / `schedule_group_attempt_members` | 模拟组尝试（含 user_id，同受 RLS 与组合外键约束）：所属组 schedule_group_id、状态（planned/active/succeeded/failed/cancelled）、两日窗口（window_start/window_end）、revision；成员行绑定 member_template_id、本次成员任务实例 task_id、合格尝试 attempt_id、group_order |
| `study_logs` | 实际时长、学习产出、成绩、掌握反馈、所属组尝试（`schedule_group_attempt_id`，可空） |
| `review_items` | 错题或薄弱项、复习到期日、处理结果；创建时原子生成关联复习任务 |
| `reminder_dismissals` | 当日提醒关闭状态 |

用户业务表包含 `user_id`。日期按 `Asia/Shanghai` 解释，时间戳存储为 UTC。

一致性、计时和规划并发按第六节执行；禁止用“旧剩余量减去本次分钟数”更新任务。

### 3. 接口边界

统一前缀 `/api/v1`，覆盖以下能力：

- 设置：读取；提醒时间直接修改；考试日、开始日、周预算、单日例外与目标作为待生效变更经预览流程原子应用。
- 今日：返回任务（组成员任务携带所属组尝试标识、窗口与状态）、预算、提醒和未处理缺口。
- 计划：读取日历、携带待生效变更生成重排预览、原子应用指定预览。
- 任务与记录：开始、暂停、提交记录、修改记录、确认完成。
- 资料：检索、关联、获取私有附件访问地址。
- 统计：返回三科进度、有效成绩和薄弱项。
- 导出：导出可移植的 JSON（含 schema_version、资料元数据，不含附件二进制；首版不提供导入恢复），以及适合阅读的 Markdown 周报。

首版不提供任意 SQL 执行接口。排程由后端计算；多表原子提交通过版本化 SQL 函数实现。

### 4. 登录、文件与数据访问

- Supabase Auth 使用个人邮箱密码登录；首版关闭公开注册。
- Fastify 通过 Supabase Auth getUser(access_token) 验证用户；请求级 Supabase 客户端使用 publishable key 和该用户 Bearer JWT 调用 PostgREST/RPC/Storage。禁止共享可变认证客户端、service_role 业务调用或直连数据库假定 auth.uid() 自动存在。
- 浏览器只使用公开客户端配置；管理密钥不进入前端。
- 云端无法读取本机绝对路径。PDF 首次使用时由本人上传到私有 Storage，再关联现有资源；未上传时明确显示“待上传”。
- 网课通过外链打开；首版不读取观看历史或自动同步第三方题库成绩。
- 网络写入失败明确显示“未保存”，支持重试；首版不实现离线写入同步。

## 四、Supabase MCP 与 Agent 标准流程

Supabase MCP 用于项目开发、数据库检查和受控维护，应用正常运行不依赖 Agent 在线。

默认限定到指定开发项目并启用只读检查；开发写入按任务范围授权。官方 MCP 支持项目范围与只读配置。[Supabase MCP 文档](https://supabase.com/docs/guides/ai-tools/mcp)

标准流程：

1. **检查**：确认项目标识、环境、现有表、迁移历史和 RLS 状态。
2. **形成变更**：输出版本化 migration、受影响对象、验证方式与恢复措施。
3. **开发环境执行**：应用迁移，导入明确标记为测试数据的样本。
4. **验证**：检查用户隔离、事务、重复提交和排程结果。
5. **生产发布**：使用已验证迁移；核对备份与影响范围后按发布授权执行。
6. **留痕**：记录迁移版本、执行结果和验证证据，撤销临时扩大权限。

Agent 不直接改写生产学习记录来“修正进度”；数据修复必须有明确目标、变更范围和审计记录。凭证使用工具授权或环境配置，不写入需求文档和仓库。

## 五、交付与验收

本文直接作为开发基线。旧备考总计划只提供目标和资料依据，不决定应用内进度；已报名、已缴费、尚未学习为已确认初始事实。

验收必须覆盖：

- 零进度初始化后，首日出现可执行任务，不显示历史完成记录。
- 把今日预算从 90 分钟改为 30 分钟，每日分配总分钟≤27（30分钟预算扣3分钟缓冲），并逐项列明被移动任务及分钟；修改经预览确认后与新计划原子生效，放弃预览时预算与日历保持原值。
- 连续三天未学，重排保留历史，显示容量缺口，不堆积到次日。
- 重复提交记录、刷新计时、两端同时应用计划，不产生重复数据或静默覆盖。
- 缺题卷和已看解析卷不进入完整卷达标统计。
- 修改学习记录后，任务进度与统计同步更新。
- 关闭网页没有外部提醒；打开后正确显示到期任务，完成后停止提醒。
- 未登录用户无法读取业务数据；另一测试账号不能访问本人记录或 PDF。
- 默认时间段能够完整安排三科模拟组：综合在周六09:00—11:30、案例在周日09:00—10:30、论文在周日14:00—16:00，不产生 GROUP_UNSCHEDULABLE；模拟论文修改作为组外必做任务另行安排。
- 手机 375px 宽度下可以完成“打开今日 → 学习 → 记录 → 完成”全过程。

实施顺序：**今日任务与记录闭环 → 重排与复盘 → 资料上传、MCP 流程和部署验收**。先让应用能指导并记录第一天学习，再完善后续功能。

## 六、可执行业务契约

### 6.1 单位、记录与唯一事实来源

模板拥有不可变 `unit_kind`、`total_units` 和 `estimated_minutes`。视频单位为原速秒，按不重叠区间记录；题组单位为题号集合；知识、案例、素材、提纲、全文与修改任务单位为预先列出的检查项 ID 集合。检查项有完成描述，首版每项等权。计时分钟仅表示投入，不表示完成量。

一个任务允许多条日志。`study_logs` 保存 id、task_id、schedule_group_attempt_id（可空；组成员任务的新日志绑定仅接受 planned/active 且窗口内的尝试——计时草稿从会话复制，手动补录由客户端携带；已绑定日志的编辑/删除不受组状态限制，schedule_group_attempt_id 与 task_id 不可变；组终态后的新补录不得追认绑定，只按普通日志提交；绑定规则见第6.2、6.5节）、revision、occurred_at、study_date、ended_at、actual_seconds、unit_ranges/unit_ids、completion_confirmed、typed_outcome、is_draft、deleted_at；ended_at 为实际结束时间（qualified_attempt 任务的最终日志与组成员日志必填）：计时证据取关联 timer_sessions.ended_at，不可编辑；手动证据必须显式提交实际结束时间；occurred_at 仅为提交时间，不参与排序与跨度判定；study_date 由 ended_at（无则 occurred_at）按 Asia/Shanghai 派生。草稿日志不计入投入统计与完成量（见第6.2节）；编辑提交整条新版本，不提交扣减量。确认字段表示用户确认这些单位达到标准；未确认日志只计时间。视频区间取并集、离散单位去重，不能累计重复提交的同一单位。题组成绩及论文成果由 typed_outcome 的版本化 TypeBox 分支验证，不把题组总分在多条日志间累加；每次独立题组尝试有唯一 attempt_id 和最终汇总记录。论文修改任务的 typed_outcome 必须包含 source_attempt_id，引用同一 goal_instance_id 下合格、未删除且不属于 failed/cancelled 组尝试的全文最终尝试，提交时由服务端校验（见第6.3、6.4节）。

权威来源为未删除日志的最新版本。任务含不可变 completion_mode：

- `unit_union`（视频、知识、案例、素材、提纲、复盘、复习等）：confirmed_units＝确认单位并集；remaining_units＝total_units−并集数量；done 当且仅当 remaining_units＝0。
- `sourced_unit_union`（论文全文修改，普通与模拟）：确认单位按 typed_outcome.source_attempt_id 分组隔离，不同来源的单位不得合并；done 当且仅当存在一个当前有效来源（同一 goal_instance_id 下合格、未删除且所属组尝试非 failed/cancelled 的全文最终尝试）且绑定该来源的未删除日志确认单位并集覆盖全部单位；remaining_units＝total_units−各有效来源并集数量的最大值（无有效来源时为 total_units）。来源失效（被编辑为不合格、删除或所属组尝试转为 failed 或 cancelled）时其支撑的单位不再计入，任务回到未完成并重新参与排程，旧日志全部保留。
- `qualified_attempt`（整卷作答、模拟三部分、限时全文）：仅当某 attempt 的最终日志满足版本化 `qualification_rules` 时判完成并核销全部剩余单位：`max_elapsed_seconds`（允许的最大用时，与 estimated_minutes 训练预算独立）、`requires_single_segment`、`manual_attestation_allowed`、`requires_all_units`（true 时最终日志须覆盖全部单位）。证据分两类：evidence=timer——连续性由数据库验证，计时会话必须恰有一个闭合 segment（requires_single_segment=true 时多 segment 不合格），elapsed≤max_elapsed_seconds；evidence=manual——无 session 的手动补录，必须提交 actual_seconds 及显式的完整、独立、连续、限时声明，仅在 manual_attestation_allowed=true 时接受，标记为用户自证。计时会话一旦被修正（提交 corrected_seconds/corrected_allocations），即丧失 evidence=timer 合格证据资格——合格判断只认原始闭合 segment 与原始 elapsed；修正后要判合格须按 evidence=manual 提交（实际 ended_at、实际秒数与完整连续限时声明，且 manual_attestation_allowed=true），原会话保留为审计信息。每个 attempt_id 最多一个当前最终版本；同一任务允许多个失败尝试；任一尝试合格即完成。分段、中止或超时的日志只保留投入与弱项证据，不扣减任务剩余量，任务保持未完成、等待一次合格尝试。

状态、剩余量只是事务内重算的缓存。删除采用软删除，编辑/删除后重算任务、目标与指标。复习项随来源日志编辑联动：错误仍存在则更新来源日志版本；错误被移除时，未开始复习退役，已开始或已完成复习保留并标记来源已撤销；学习日期修改时，仅未开始复习按新 study_date 重算到期日；错误所属主题修改同步复习项主题。复习任务保留已有日志；来源日志整体删除时，未开始复习退役，已开始或完成复习保留并标记来源已撤销。

每个任务有 `task_revision`，修改日志同时校验 log_revision 和 task_revision；冲突返回 409 REVISION_CONFLICT 及当前版本，客户端重新读取，不自动覆盖。完成命令必须引用具体确认单位，不接受独立的 done=true。所有写入含 Idempotency-Key，按用户、操作、key 唯一；同 key 同请求返回原结果，同 key 不同请求返回409；幂等记录与业务变更同事务保存。

### 6.2 计时器

增加 `timer_sessions(id,user_id,task_id,schedule_group_attempt_id,source_attempt_id,state,revision,controller_id,started_at,ended_at)` 和 `timer_segments(id,session_id,start_at,end_at)`。schedule_group_attempt_id 可空；组成员任务开始计时必须携带该标识，或由服务端按该任务唯一的非终态（planned/active）组尝试成员行推导绑定，无法唯一推导、组已终态或当前本地日期在窗口外时返回 422 GROUP_ATTEMPT_CLOSED，不等提交日志时才拒绝。source_attempt_id 可空；sourced_unit_union 任务开始计时时继承被执行计划项的 source_attempt_id（无计划项则为空），stop 草稿一并复制，提交正式日志时 typed_outcome.source_attempt_id 必须与之相等，避免提交时临时换绑。时间由数据库生成；每个用户最多一个 running 或 paused 会话（部分唯一索引），每个会话最多一个未闭合 segment。

状态机：start→running；pause 关闭 segment→paused；resume 新建 segment→running；stop 关闭 segment→stopped。stop 不自动完成任务，也不止预填内存表单：stop 在同一事务内持久化一条草稿日志（is_draft=true，含 task_id、session_id、schedule_group_attempt_id（复制自会话，可空）、source_attempt_id（复制自会话，可空）、actual_seconds、ended_at（取会话 ended_at）、study_date（由 ended_at 按 Asia/Shanghai 派生），无确认单位），session_id 在日志中唯一，重复 stop 返回已有草稿，避免同一计时重复入账。草稿不计入投入统计与完成量；提交＝补全单位与成果后转为正式日志（复用第6.1节日志规则），放弃＝删除草稿。今日与学习记录页显示待提交草稿，停止后刷新、关闭页面或更换设备均可恢复提交；来自超4小时会话的草稿须先确认或修正 corrected_seconds 才能提交。累计时长为闭合区间长度之和，运行时只在展示中加 now−start_at，不每秒写库。

修改计时命令要求 session revision 和 controller_id。其他设备只能读取，点击“接管”后在事务内更新 controller_id/revision，旧设备命令409。接管不新建 segment。暂停会话仍占用唯一活动名额，必须恢复或结束才能启动其他任务。

连续 running 段超过4小时，展示“请核对时长”，禁止直接提交日志，须明确确认或修正；不会自动计入学习成果。事后修正保留原计时段，在日志记录 corrected_seconds、修正理由，以及按中国时区本地日拆分的 corrected_allocations（每项为 study_date＋seconds，总和必须等于 corrected_seconds，跨午夜记录必须给出每日归属）；投入统计按 corrected_allocations 归属，无修正时按原计时段按日界拆分。经修正的会话不再具备 evidence=timer 合格证据资格，须转 evidence=manual（见6.1）。

### 6.3 目标库与统计口径

增加 `study_goals(id,user_id,kind,target,revision)`，默认如下。目标修改不直接生效：作为待生效变更进入预览，应用预览时与新计划原子生效并递增规划修订号。降低目标：已完成与已开始（含活动计时）实例一律保留，仅将未开始的多余必做实例按稳定实例序号从大到小撤销为 retired；任务实例只退役、不物理删除，历史 plan_versions/plan_items 引用保持有效，当前计划在应用新计划前不受任何实例变动影响。提高目标：按稳定序号生成新实例并纳入同一新计划。超出种子支持范围返回422，不能生成空壳任务。

| 目标 | 默认必做量 | 达成依据 |
| --- | ---: | --- |
| 年度题组 | 7次 | 7个不同年份/考期的最终确认尝试，须披露题目完整性；重复同卷不重复计数 |
| 案例主题 | 6个，另2个选做 | 不同主题均完成独立作答和失分修改 |
| 论文提纲 | 6份 | 不同题目ID的提纲检查项全部确认 |
| 论文全文 | 2篇，另1篇选做 | 不同题目ID的限时全文合格且对应修改任务完成；修改须以 typed_outcome.source_attempt_id 绑定同一目标实例下合格、未删除且不属于 failed/cancelled 组尝试的全文最终尝试；来源尝试被编辑为不合格、删除或所属组尝试转为 failed 或 cancelled 时，该实例达成同步撤回，修改日志本身保留 |
| 三科模拟 | 1轮 | 同一成功 schedule_group_attempt 内综合、案例、论文三条合格最终日志齐全且 ended_at 的本地日期跨度≤2天（见第6.5节）；失败的组尝试保留日志但不计数，重排创建新组尝试并为全部成员生成新任务实例（含已合格成员）；失败或已取消组尝试的日志计入投入与弱项统计，但不计入模拟目标与全文目标；组成功状态随成员日志编辑/删除重算，不再成立时同步撤回；同一模拟目标最多一个成功组尝试参与计数，多个成功尝试按（组完成时间＝三条成员合格日志 ended_at 之最晚者升序、尝试 ID 升序）稳定选定，其余保留证据不重复计数 |

目标可调范围：题组1—7、案例1—8、提纲1—6、全文1—3、模拟1轮。跨目标不变量在生成预览前校验，违反返回 422 GOAL_INVARIANT_VIOLATION 且不落库任何变更：提纲目标≥全文目标；任何保留必做任务的依赖闭包必须全部保留（退役某任务前递归检查，不得使保留任务的依赖失效）。实例调整按稳定实例序号确定性执行：降低目标按序号从大到小退役未开始实例，提高目标按序生成新实例。达标正确率固定70%，与任务数量目标独立。主题固定为需求分析、数据库设计、系统规划与可行性、架构设计、性能可靠性、安全、项目管理、大数据；前6必做、后2选做。修改案例数量时按此顺序取前N。三科模拟为固定1轮不可调，由 schedule_group 与 schedule_group_attempt 保证（见第6.5节）。

统计时间窗口：投入/周完成率为当前中国时间自然周；产出目标为本次备考全期；弱项为预览 as_of_date 前14个日历日（含当天）；“最近三套”为全期符合条件的尝试按 ended_at（实际结束时间）降序、attempt_id 升序取3个不同考期。

周完成率按承诺分钟加权，不把异构单位（视频原速秒与少量检查项）直接相加，且核对具体承诺单位而非仅数量：每周首次应用计划冻结各 task_id 的 baseline_confirmed_set（周初已确认单位集合）、target_set(t)＝本周计划项承诺单位并集−baseline_confirmed_set、本周承诺分钟 committed_minutes(t)（本周该任务各 plan_item 分钟之和）。本周完成 progress(t)＝|current_confirmed_set∩target_set(t)|——确认后900秒替代承诺的前900秒不视为完成；完成比例 r(t)＝progress/|target_set|，target_set 为空的任务不参与。周完成率＝Σ(committed_minutes(t)×r(t)) / Σcommitted_minutes(t)；分母为0时显示“暂无承诺”，不显示100%。周内新加入任务追加其基线与承诺；移走、删减或重排不缩小分母，被移走任务仍按冻结值计入；同一任务不重复计数；单任务完成比例封顶1，不用超额部分抵补其他任务；跨周任务只计落在本周 target_set 内的确认单位，历史单位不进本周成绩。sourced_unit_union 任务的 baseline_confirmed_set、target_set 与 current_confirmed_set 按 (task_id, source_attempt_id) 维度冻结与核对，不把不同来源的确认单位重新合并；来源以本周计划项的 source_attempt_id 为准。结束周以当时日志版本保存统计快照；后续历史修正生成新统计版本并标记已修正。

完整卷正确率：仅75题、未见答案、独立连续限时且有最终记录的卷；取3卷正确率的算术平均，少于3卷显示证据不足。完整性和限时为用户确认的训练证据，不宣称外部验证。案例覆盖为已达成主题数/当前目标主题数，同时显示完整8主题矩阵。

弱项分数按主题计算，未知主题分数为50并标记“暂无证据”。已知分数 W＝round_half_up(100×(0.5E+0.3M+0.2R))：E＝窗口内错题数/有效作答题数，缺题数据缺失时用0.5；M＝最新掌握反馈的未知1、易混0.5、掌握0，缺失用0.5；R＝min(到期未完成复习数,3)/3。一个题目只能归一个主主题；同时间反馈按log_id字典序取最后一个。逾期不会无限增权。

### 6.4 版本化种子规范

实现产物为一个 `seed-manifest.json`，版本 `sysanalyst-2026-v1`，使用 RFC 8785（JCS）规范化 JSON 的 SHA-256 校验，任何实现必须产生相同摘要；每个版本不可变。初始化按(user_id,logical_template_id,instance_index)唯一（不含种子版本），重复初始化或种子升级不重复生成。升级须预览差异并按 keep/add/replace/retire 处理：模板内容未变化 keep；新增逻辑模板 add；同一 logical_template_id 内容变化 replace——未开始实例以新模板内容替换并保留稳定实例序号，已开始（有日志或活动计时）或已完成实例保留原内容与日志并标记旧种子版本；新版本中消失的逻辑模板 retire——未开始实例撤销为 retired，已开始或已完成实例保留并标记来源模板已退役。升级预览逐项列出四类清单及影响任务数，确认后与新计划在同一事务内生效。

每条模板必须有：id（版本内唯一）、logical_template_id（跨版本稳定字符串）、sort_index（唯一整数）、subject、topic_id、kind、title、resource_ids、source_locator（可null）、unit_kind、unit_items或total_units、estimated_minutes（正整数）、splittable、min_chunk_minutes、dependencies（模板ID列表）、completion_mode（unit_union/qualified_attempt/sourced_unit_union；sourced_unit_union 仅用于论文全文修改）、qualification_rules（qualified_attempt 必填：max_elapsed_seconds、requires_single_segment、manual_attestation_allowed、requires_all_units）、goal_kind/goal_index/goal_instance_id（可null；goal_instance_id 为目标实例主键，goal_index 仅用于普通实例按序号稳定选择）、required、release_offset_days（发布日期＝start_date+release_offset_days，到达前不具备候选资格）、weekend_only（布尔，true 时仅排周六日）、priority_class（0普通/1首周论文素材/2首个周末诊断）、deadline_offset_days（可null，截止日＝start_date+deadline_offset_days）、fixed_weekday（可null，固定星期几）、schedule_group_id（可null）、group_order（可null）、group_max_span_days（可null）、completion_criteria。priority_class 越大越先排，priority_class>0 的任务先于一切普通排序规则占用容量。禁止环、缺失依赖、空完成标准和同版本内容漂移。组字段结构校验：同组 group_order 唯一且从 1 连续编号；同组 group_max_span_days 完全一致；schedule_group_id 为空时 group_order 与 group_max_span_days 必须同空，非空时二者均必填；同组 required 必须一致，组按该统一属性整体参与必做/选做两阶段排程；组成员的 dependencies 只能指向组外任务或 group_order 更小的同组成员，不得指向 group_order 更大者或任何（传递）依赖本组成员的模板，保证组可在其后置任务之前整体完成。

种子展开规则：公共课恰为56条已核对视频，保留BV号、原速秒和合集顺序；视频任务预算为ceil(原速秒/90)，允许拆分，最小块25分钟。每个视频任务在种子中附带一个10分钟知识回忆任务（1个检查项：闭卷回忆并写出要点；不可拆；required 与视频一致），依赖该视频任务；回忆时间不再混入视频 estimated_minutes，避免固定开销被按视频秒数线性摊薄、或视频区间确认完毕后整段回忆被跳过。10个公共模块各1个30分钟框架检查任务；企业信息化战略补1个60分钟任务。选做课只有在用户提升为必做时才先于其他选做排入。

7个年度题组实例各含独立作答150分钟（不可拆、completion_mode=qualified_attempt，max_elapsed_seconds=10800，requires_single_segment=true，manual_attestation_allowed=true，requires_all_units=true）和复盘90分钟（可拆25分钟），复盘依赖作答。具体卷从资料库按考期升序选择未见答案的卷，最新完整卷保留模拟；完整性/考期未核对时阻断该实例并报告 RESOURCE_UNVERIFIED，不猜造卷号。目标计数不因资源待核对而缩小。

8个案例主题各作答60分钟＋修改30分钟；论文素材60分钟、priority_class=1、deadline_offset_days=6；6份提纲各45分钟依赖素材；普通全文实例3个（goal_instance_id=essay-regular-1 至 3，goal_index=1—3 用于按序号稳定选择）：各含限时全文120分钟（completion_mode=qualified_attempt，max_elapsed_seconds=8100，requires_single_segment=true，manual_attestation_allowed=true，requires_all_units=true）依赖同题提纲、修改60分钟（completion_mode=sourced_unit_union）依赖全文，第3篇选做。三科模拟为一个 schedule_group（同 simulation_id、group_max_span_days=2，意为最多两个不同本地日期），仅含三项考试任务：模拟综合150分钟（group_order=1，weekend_only=true，completion_mode=qualified_attempt，max_elapsed_seconds=10800，requires_single_segment=true，manual_attestation_allowed=true，requires_all_units=true）、模拟案例90分钟（group_order=2，weekend_only=true，completion_mode=qualified_attempt，max_elapsed_seconds=6300，requires_single_segment=true，manual_attestation_allowed=true，requires_all_units=true）、模拟论文120分钟（group_order=3，weekend_only=true，completion_mode=qualified_attempt，max_elapsed_seconds=8100，requires_single_segment=true，manual_attestation_allowed=true，requires_all_units=true）；模拟论文修改60分钟（completion_mode=sourced_unit_union）为组外必做任务、依赖模拟论文、weekend_only=false；该依赖按 goal_instance_id 解析而不绑定固定任务实例（见6.5），任一组尝试的模拟论文合格后即可安排在其后任何日期，提交时以 source_attempt_id 绑定该尝试。模拟论文与模拟论文修改共同构成一个全文目标实例（goal_instance_id=essay-simulation），完成两件后才计入全文目标；普通全文实例 goal_instance_id=essay-regular-1 至 3；普通必做全文数量＝max(0, 全文目标−1)。全文修改（普通与模拟）完成确认时 typed_outcome 必须包含 source_attempt_id，指向同一 goal_instance_id 下合格、未删除且不属于 failed/cancelled 组尝试的全文最终尝试；来源尝试被编辑为不合格、删除或所属组尝试转为 failed 或 cancelled 时，该目标实例达成状态同步撤回，修改日志本身保留。上述时长是训练预算，正式规则核对后通过新种子版本调整，不能当作官方时限承诺。除视频、题组复盘和全文修改外，其余任务不可拆；全部普通任务 release_offset_days=0，普通论文素材与其他必做同弱项分数时 sort_index 排在其前；首个诊断150分钟、priority_class=2、weekend_only=true（预约首个可容纳周末时段），普通全文与模拟三项考试任务 weekend_only=true，其他任务 weekend_only=false。

复习项由用户在日志显式标记的稳定 error_id 创建，选择题到期日＝study_date+3，案例＝+2。创建复习项的同一事务内生成对应 tasks 实例（kind=复习，1个检查项：重新作答并说明错因；估时25分钟、不可拆、required=true），tasks.source_review_item_id 唯一关联复习项；复习的排程、日志、完成确认、revision 与并发规则完全复用任务模型。复习完成不会无限自动派生下一项。复习重复触发按(source_log_id,error_id)去重。周日复盘20分钟、不可拆、fixed_weekday=7，排程时按每个备考周生成一次固定实例，只排周日。

### 6.5 排程算法及预览一致性

固定时区 Asia/Shanghai，不提供时区配置；exam_date/start_date/plan_date 为date。截止日＝exam_date−1。首次生成要求start_date≥today且exam_date>start_date；已有计划改到today或更早时允许保存，但不生成未来学习项并显示考试窗口已结束。剩余天数＝max(0,exam_date−today)，不加1。默认时间段：周一至周四20:00—21:30，周五20:00—21:00，周六09:00—12:00、14:00—15:00，周日09:00—10:30、14:00—16:30；用户可修改非重叠时间段。默认容量必须能容纳三科模拟组：周六扣除末段缓冲后得连续段180分钟容纳模拟综合（150分钟），周日扣除末段缓冲后得90、126分钟容纳模拟案例（90分钟）与模拟论文（120分钟）。单日分钟快捷修改从现有时间段尾部裁剪；增量延长最后一段，不得跨午夜或重叠。

每天预算B为时间段总分钟，缓冲ceil(B/10)，从末段尾部向前扣；可分配C=B−缓冲。周六默认B=240、缓冲24从下午段扣，得180＋36；周日默认B=240、缓冲24从下午段尾部扣，得90＋126。既有活动任务固定的剩余分配先占容量，若超过新C则不新增任务，并报告 FIXED_OVERFLOW，不能悄悄移动活动任务。今天已消耗时段按预览时刻裁掉，历史日期不参与重排。

定义累计分钟边界 b(n)＝round_half_up(estimated_minutes×n/total_units)，满足 b(0)=0、b(total_units)=estimated_minutes。预计剩余分钟＝estimated_minutes−b(c)，c＝当前已确认单位数，done为0；实际耗时不直接扣此估值。此为剩余分钟的唯一规则；排程运行（含预览）内以规划游标 p 推广（见下文拆分规则）：运行内剩余分钟＝estimated_minutes−b(p)，未安排输出的 remaining_minutes 同按此给出。sourced_unit_union 任务先按（已确认单位数降序、来源全文日志 ended_at 升序、attempt_id 升序）选定本次规划来源 S；c、未确认单位集合与剩余分钟均相对 S 计算；同一预览内同一任务固定使用同一来源 S，plan_items 记录 source_attempt_id（见下文落库规则）。算法输入含固定 as_of_timestamp、seed_version、algorithm_version=1、完整业务快照，不在算法内读取当前时间或随机数。

排序元组升序：(required?0:1,到期复习?0:1,due_date或9999-12-31,-W,sort_index,task_id)。复习 due_date 到达前不具备候选资格；plan_date 早于任务发布日期（start_date+release_offset_days）时同样不具备候选资格。特殊约束先于普通排序执行：priority_class>0 的任务最先占用容量——诊断预约首个可容纳的周末时段，论文素材在 start_date+deadline_offset_days 之前排完，无法满足时显式报告 PRIORITY_UNSCHEDULABLE，不以普通排序搪塞；deadline_offset_days 任务的分配日期不得晚于截止日，逾期报告 DEADLINE_MISSED；fixed_weekday 任务只排对应星期；schedule_group 视为一个候选：组排序键取 group_order 最小成员的排序元组；当组成为候选时整体尝试——以 group_order 升序在跨度窗口内按 (plan_date, start_time) 字典序搜索完整组合，选择第一个完整解，无解才返回 GROUP_UNSCHEDULABLE，不产生半组日历；group_max_span_days=2 意为最多两个不同本地日期（max_date−min_date≤1），不接受三日跨度；应用 RPC 校验成员按 group_order 时间先后不逆序。组执行以持久化的 schedule_group_attempts / schedule_group_attempt_members 管理（见3.2）：应用含组的计划时在同一事务创建组尝试（同组按序号递增），写入 schedule_group_id、成员任务实例 task_id、group_order 与两日窗口（window_start＝最早成员 plan_date，window_end＝window_start＋group_max_span_days−1），成员 plan_items 携带 schedule_group_attempt_id。组成员任务的新日志绑定仅接受属于本用户、状态为 planned/active 且 study_date（由 ended_at 派生，见6.1）落在窗口内的尝试，否则返回 422 GROUP_ATTEMPT_CLOSED；绑定被拒时可去除该标识按普通日志提交，仅计投入与弱项；组终态后的新补录一律不得追认绑定终态尝试。已绑定日志的 PATCH/DELETE 不受组状态与窗口限制（schedule_group_attempt_id 与 task_id 不可变），修改后同事务重算组状态；窗口内产生并已绑定的计时草稿即使组后来惰性结算为 failed 仍可转正式日志，是否合格由原计时证据与实际结束时间判断，可据以使组重算回 succeeded。成员取得合格最终日志时其 attempt_id 写入成员行，据此证明三条日志属于同一次模拟，跨设备提交以服务端下发的标识为准；成员行 attempt_id 为派生缓存：在该成员绑定本尝试的合格最终日志中按（ended_at 升序、attempt_id 升序）选定首条，成员日志新增、编辑或删除后同事务重选，只要仍存在合格候选即维持组状态，仅当无任何合格候选时组状态才回退。状态机：planned（尚无成员活动计时、草稿或非草稿日志）→ active（任一成员开始计时、产生草稿日志或提交非草稿日志；计时绑定规则见6.2）→ succeeded（全部成员在窗口内取得合格最终日志）；planned/active 在 window_end 过后仍有成员未合格时转 failed；planned 尝试被重排移动或移除任一成员计划项时转 cancelled；planned/active 也可经取消命令（POST /group-attempts/:id/cancel，见7.2）转 cancelled，成员有活动计时器时须先停止；cancelled 为单向终态：已绑定日志保留且可编辑，但不再回算为其他状态。无后台状态推进服务：窗口过期在读取今日、生成预览或提交日志的事务内惰性结算，结算与其他变更一样锁 planning_state 并递增 revision。惰性结算与同请求后续校验冲突时（如结算为 failed 后新绑定被拒绝），RPC 必须提交结算（状态与 revision 递增已持久化）并以结构化领域结果返回拒绝原因，不得用数据库异常表达 422 而回滚结算；Fastify 在事务提交后将领域结果映射为 HTTP 422。active 尝试的成员剩余分配视同固定分配：先占容量、超出报告 FIXED_OVERFLOW、不参与自动移动。failed 且模拟目标未达成时，后续重排创建新组尝试，并为全部成员按各自模板生成新任务实例（含已合格成员，允许重做；实例按 (user_id,logical_template_id,instance_index) 递增序号，不占目标名额，参与种子升级既有规则），不复用已 done 成员的任务进度；旧任务实例与其日志保留为失败尝试证据，不参与排程；其日志计入投入与弱项统计，但不计入模拟目标与全文目标；planned 取消后重排可复用尚无日志的原成员实例。早期组尝试经日志修正重新满足成功条件时按当前日志重算回 succeeded：此时模拟目标已达成，同组较新的重试尝试仍为 planned 的在同一事务自动转 cancelled，其成员未来计划项不再出现在今日待办、不再接受计时与日志绑定，并在下一次重排移除；已 active 的保留其日志与活动计时，剩余成员不再计入必做缺口、不再派生新重试，用户可继续执行（正常计投入与弱项）或经取消命令放弃，剩余计划项在下一次重排移除。恢复成功的早期尝试之后再次因日志修改失效、模拟目标重新未达成时：优先恢复仍处窗口内的 active 重试——其剩余成员重新计入必做缺口并参与后续重排；该重试已过窗口的先惰性结算为 failed，再按既有规则创建新尝试；已自动 cancelled 的 planned 重试不复活，保持审计单向。组状态与任务状态同为事务内重算的缓存而非不可逆标记：成员日志编辑或删除后同事务重算组状态与模拟目标，合格条件不再满足时 succeeded 回退为 active（窗口内）或 failed（窗口外），目标计数同步撤回、日志本身保留。全文修改任务（普通与模拟）除模板依赖外，候选资格要求同一 goal_instance_id 下存在合格、未删除且不属于 failed/cancelled 组尝试的全文最终尝试；对组成员全文模板，该尝试可来自任一重试实例。模拟目标只接受同一成功组尝试内三个成员的合格最终日志，且三条日志 ended_at 的本地日期跨度≤2 天。依赖须真实完成或已在当前预览安排完全部剩余量；后继开始时间必须晚于所有依赖的结束时间。按日期、时段开始时间升序，反复扫描排序候选，取首个合法且能放入当前空段的任务；大任务放不下时继续找后续任务，不能阻塞整天。

可拆任务的分钟由承诺单位按累计边界 b(n)（定义见上文）反推，保证任意拆分与重排后总分钟守恒。排程运行（含预览）内对每个可拆任务维护规划游标 p＝已确认单位数＋本次运行已承诺单位数，初值为快照已确认单位数 c，同一任务的各块跨时段、跨日期沿用同一游标。设 cap＝min(estimated_minutes−b(p),60,当前空段分钟)；可容纳单位数 k_cap＝max{ k≥0 | b(p+k)−b(p) ≤ cap }，最小块单位数 k_min＝min{ k≥1 | b(p+k)−b(p) ≥ min_chunk_minutes }（k_min 超过剩余单位数 total_units−p 时取剩余单位数，即尾块允许不足下限）。k_cap=0 或 k_cap<k_min 且本块不能承诺全部剩余单位时跳过该时段；否则承诺单位数 k＝k_cap（若剩余单位全部可承诺则取剩余单位数，即尾块），allocated_minutes＝b(p+k)−b(p)，分配后 p+=k。同一预览内同任务各块按游标累计：est=61、2单位拆两块为 b(1)−b(0)=31 与 b(2)−b(1)=30，合计61守恒；运行内预计剩余分钟为 estimated_minutes−b(p)，不随拆分膨胀。不可拆任务必须在同一剩余连续时段放完。排程不跨时段拼接限时任务。完成分配后立即重新评估依赖候选，直至当日无可放入项。每条分配必须完整落库为 plan_items：task_id、schedule_group_attempt_id（组成员分配必填，其余为null）、source_attempt_id（sourced_unit_union 任务必填且等于规划来源 S，其余为null）、plan_date、start_time、end_time、sequence（当日从1连续编号）、allocated_minutes、committed_unit_ranges/unit_ids、reason；同日分配不重叠且落在当日时段内，后继分配开始时间晚于其依赖分配的结束时间。承诺单位按稳定单位顺序确定性切分：从“尚未确认且本次运行尚未承诺”的有序单位集合顺序切片（sourced_unit_union 任务按规划来源 S 的确认集合判定“尚未确认”），不是从全部单位前缀切片；任何非最终 plan_item 必须至少承诺一个单位；manifest 校验拒绝 total_units < ceil(estimated_minutes/60) 的 splittable 模板（首个 60 分钟块无法承诺一个单位）。本周各块并集即第6.3节 target_set 来源，页面据此显示每段计划的具体完成范围。weekend_only 任务只扫描周六/日；“首个可用周末”即首个能容纳任务的周末时段。

先完整排所有必做，再将选做填入剩余空隙；选做不得占用导致必做被顺延的容量。未安排输出 task_id、remaining_minutes 和原因：DEPENDENCY_BLOCKED、RESOURCE_UNVERIFIED、NO_CONTIGUOUS_SLOT、CAPACITY_SHORTFALL、AFTER_EXAM、FIXED_OVERFLOW、GROUP_UNSCHEDULABLE、DEADLINE_MISSED、PRIORITY_UNSCHEDULABLE。必做缺口为未分配必做分钟总和（包括依赖阻塞项；schedule_group 未排时计入全部必做成员分钟），选做另列不计目标缺口。

增加 `planning_state(user_id,revision)`，设置/例外/目标、日志、计时活动状态、复习、组尝试（含状态机惰性结算）、任务/种子/资料可用性和计划应用均在变更事务内锁此行并递增revision；所有相关RPC遵循相同锁顺序。快照通过单个RPC读取同一数据库快照及revision。`plan_previews` 服务端保存输入revision、as_of、算法版本、待生效变更（设置/目标/单日例外）、完整分配（含组成员项的组尝试绑定）与差异；重试组的新成员在预览中以 prospective_task_spec 表示（预生成 task_id UUID、logical_template_id、instance_index、schedule_group_id、group_order、新组尝试序号），预览不产生任务行；客户端应用仅提交preview_id和幂等key，不回传排程内容；预览生成与应用均按幂等键复用同一批预生成 ID。

预览持久化RPC与应用RPC必须独立校验任务所属、分配整数分钟、起止时间不重叠且落在当日时段内、sequence 连续、预算、依赖先后（后继开始晚于依赖结束）、weekend_only/fixed_weekday/截止日遵守、schedule_group 完整且跨度合规、组成员分配的组尝试绑定一致且按 group_order 时间不逆序、sourced_unit_union 分配的 source_attempt_id 与规划来源一致且承诺单位属于该来源未确认集合、承诺单位范围单调不重叠且与分钟换算一致、不重复分配等约束；引用尚不存在任务的重试组分配必须携带 prospective_task_spec，校验其模板属于当前种子、instance_index 为该 (user_id,logical_template_id) 的下一个可用序号且不与现有任务冲突；即使认证用户绕过Fastify直接调用RPC也不能提交违规日历。服务端保存预览不能被authenticated直接INSERT/UPDATE；预览写入RPC同样校验输入revision。

应用事务锁planning_state，检查所属用户、revision、预览未应用、生成未超过15分钟且未跨本地日期；冲突409 PREVIEW_STALE，不改变任何计划。活动计时器跨时段导致预览分配的时间已过去时同样拒绝。原子写待生效设置/目标/单日例外、plan_versions/items（含起止时间、sequence 与承诺单位）、组尝试变更（新建尝试及其成员行、取消被重排的未开始尝试；重试组先按 prospective_task_spec 插入任务实例与成员行，再插入引用其 task_id 的 plan_items；放弃或过期的预览不产生任何任务行）、周承诺基线和新revision，标记预览已应用；plan_versions 同时保存生成时的完整输入快照（设置、例外、目标、seed_version、algorithm_version、as_of），活动计划始终可对照其生成口径；相同幂等请求返回原结果。

### 6.6 排程黄金样例

下列fixture固定同一快照、起点为当天00:00；均无固定任务、资源有效，未说明的任务均必做、不可拆、W=50、无依赖，ID字典序与sort_index一致。输出必须精确一致，包括空闲和未安排量。

| 样例 | 输入 | 唯一期望输出 |
| --- | --- | --- |
| 缓冲与拆分 | 9/7单段20:00—20:30；考试9/8；A有40个单位、需40分钟、可拆最小25；B需10分钟 | C=27；cap=27，k_cap=27≥k_min=25；A分配20:00—20:27（27分钟、承诺27单位）；A缺13分钟、B缺10分钟；必做缺口23 |
| 依赖与顺序 | 9/7单段20:00—21:00；考试9/8；A30分钟，B20分钟依赖A，C10分钟 | C=54；A20:00—20:30，B20:30—20:50；空闲4；C缺10 |
| 连续时段 | 9/12两段09:00—09:30、14:00—14:30；考试9/13；A40分钟 weekend_only；B25分钟 | C=54，分段30与24；B09:00—09:25；A不排，NO_CONTIGUOUS_SLOT，缺40 |
| 弱项优先 | 9/7单段20:00—21:00；考试9/8；A/B各30，A的W=20、B的W=80 | B20:00—20:30；A缺30；不得为利用率改排A |
| 复习顺延 | 9/7预算0，9/8单段20:00—20:30；考试9/9；R复习25分钟due=9/7，A25分钟 | R在9/8 20:00—20:25；A缺25；R原due不改 |
| 模拟组正解 | 9/12（周六）09:00—12:00、14:00—15:00；9/13（周日）09:00—10:30、14:00—16:30；考试9/20；模拟组三部分 weekend_only：综合150（order1）、案例90（order2）、论文120（order3） | 综合9/12 09:00—11:30；案例9/13 09:00—10:30；论文9/13 14:00—16:00；周六09:00—12:00段空闲30、14:00—15:00段空闲36；周日14:00段剩余空闲6（16:00—16:06）；组跨度2天合规；无 GROUP_UNSCHEDULABLE |
| 单位边界 | 9/7单段20:00—20:54；考试9/8；B任务5个等权检查项、预计60分钟、可拆最小25 | 缓冲ceil(54/10)=6，C=48；b(n)=round_half_up(60n/5)=12n；k_cap=4≥k_min=3；分配20:00—20:48（48分钟=b(4)−b(0)）承诺前4项；剩1项12分钟未排，CAPACITY_SHORTFALL |

## 七、授权与外围流程闭环

### 7.1 数据库边界

所有用户业务表SELECT使用 `auth.uid()=user_id` RLS，外键采用(user_id,id)组合避免跨用户关联；视图必须security_invoker。客户端不能传user_id，RPC内部从auth.uid()取得并要求非空。撤销anon全部业务权限、authenticated直接表写权限，仅开放必要读取及业务RPC。

事务RPC采用SECURITY DEFINER时必须承认其可绕过RLS：显式校验每个关联对象所属用户，固定search_path为空、全限定表名、无动态SQL，撤销PUBLIC/anon execute，仅授予authenticated；派生进度、revision和并发校验必须在RPC内执行，不能仅放Fastify。普通读走RLS，受限写走显式授权RPC，禁止声称所有路径均由RLS自动保护。[RLS授权说明](https://supabase.com/docs/guides/database/postgres/row-level-security)、[函数权限说明](https://supabase.com/docs/guides/database/functions)。

### 7.2 计时、记录与计划接口

所有路径前缀 /api/v1，写接口使用第6.1节幂等约定，错误体为{code,message,current_revision?,details?}；401未认证，404不可见对象（不泄露他人对象存在性），409冲突，422规则校验失败。

- POST /timers，GET /timers/active；POST /timers/:id/pause、resume、stop、takeover：均返回状态、revision、服务端时间与累计秒数，stop 额外返回持久化草稿日志 id。组成员任务开始计时必须携带 schedule_group_attempt_id 或由服务端按唯一非终态成员行推导绑定，组已终态或当前本地日期在窗口外返回 422 GROUP_ATTEMPT_CLOSED；stop 生成的草稿日志复制该标识。
- POST /tasks/:id/logs，PATCH/DELETE /logs/:id：携带预期日志/任务版本；组成员任务新日志必须携带 schedule_group_attempt_id，服务端校验尝试归属、状态与窗口后绑定；已绑定日志的 PATCH/DELETE 不受组状态与窗口限制，schedule_group_attempt_id 与 task_id 不可变，修改后同事务重算组状态；窗口内已绑定的计时草稿在组转为 failed 后仍可转正式日志，组终态后的新补录不得追认绑定；返回重算进度、目标（含组尝试状态与模拟目标）和新revision。
- POST /group-attempts/:id/cancel：携带预期 revision 与幂等键，将 planned/active 组尝试转为 cancelled；成员有活动计时器时须先停止，否则409；cancelled 为单向终态，已绑定日志保留可编辑，成员未来计划项不再可执行并在下一次重排移除；模拟目标未达成时后续重排按既有规则创建新尝试。
- GET /settings、/goals；PATCH /settings 仅含即时生效项（提醒时间）；考试日、开始日、每周时间段、单日例外与目标经 POST /plans/previews 的 changes 提交，随应用原子生效并返回新规划revision。
- POST /plans/previews（可携带 changes：settings/goals/day_overrides），GET /plans/previews/:id，POST /plans/previews/:id/apply；GET /plans、/today（含待提交草稿日志；组成员任务携带 schedule_group_attempt_id、窗口与状态，读取今日时惰性结算窗口过期；修改任务计划项携带 source_attempt_id，页面据此显示正在修改哪篇全文）、/statistics、/logs（支持 is_draft 过滤）。
- POST /reviews（引用来源日志error_id，同一事务创建复习任务并返回 task_id），POST /reviews/:id/complete（定位关联任务后按第6.1节提交确认日志，返回重算进度）；PUT /reminders/:id/dismiss（作用于本地日期）。

### 7.3 私有附件

仅支持PDF，单文件≤50MiB，Storage bucket同样限制。`resource_files` 保存owner、resource_id、object_key、状态pending/ready/deleting、大小、sha256与版本；路径由服务器产生 user_id/resource_id/file_uuid.pdf，不使用客户端路径。

POST /resources/:id/uploads：验证所有权，生成pending行并返回上传目标。浏览器携带用户JWT上传私有Storage，Storage INSERT策略只允许本人pending记录对应的新key，禁止覆盖已ready对象。Storage SELECT仅允许本人关联文件，DELETE仅允许本人pending或deleting文件；不授予ready对象UPDATE。所有策略同时匹配bucket、完整object_key、owner和资源关联。这里使用JWT上传，不生成可转交的签名上传URL。

POST /resources/:id/uploads/:fileId/complete：再次验证所有权，通过用户JWT读取对象并核对大小、PDF头与SHA-256后原子标ready。POST /resources/:id/files/:fileId/download-url：核对resource和file均属于本人且ready后签发60秒下载URL；链接有效期内具有持有者访问语义，前端不持久存储。[Storage访问控制](https://supabase.com/docs/guides/storage/security/access-control)。

替换＝上传新版本并确认成功后切换当前引用，原版本保持至用户删除；DELETE /resources/:id/files/:fileId 将其标deleting，调用Storage删除，成功后清理行，失败保留可重试状态。删除当前文件后资源显示待上传。取消/失败上传使用同一删除接口；不靠数据库事务假定Storage也能回滚。

### 7.4 账号与导出

部署人员通过Supabase Dashboard创建并邀请唯一首个账号，关闭公开注册；配置Auth专用SMTP和允许的邀请/密码恢复跳转URL。邀请链接进入设置密码页后登录；忘记密码使用Supabase Auth恢复邮件，恢复页调用updateUser修改密码。认证邮件不属于学习提醒，不增加邮件提醒功能。初始化种子在首次登录确认设置后幂等执行；没有账号管理API。

GET /exports/json：含schema_version=1、exported_at、种子版本、设置/目标/任务/计划/日志/复习/组尝试及成员绑定/资料元数据；不含JWT、密码、签名URL和附件内容。GET /exports/weekly?week_start=：Markdown周报。明确标注“可移植导出，首版不支持应用内导入恢复”；数据库备份与恢复由运维流程承担，不能拿此JSON冒充备份。

## 八、补充验收门槛与审阅对照

排程七个黄金样例应成为固定测试；同输入重复运行及乱序输入必须得到相同输出。额外测试循环依赖拒绝初始化、manifest 组字段结构违规（group_order 重复或不连续、同组跨度不一致、schedule_group_id 与其他组字段缺失或多余、同组 required 混合、成员依赖指向组内后置成员或组的后置任务）拒绝初始化、依赖未完成不提前排、exam_date提前一天后重新报告缺口、期外任务不生成、复习due不随顺延修改、缓冲ceil边界、活动任务超预算、release_offset_days 到达前不排、weekend_only 任务不落在工作日、承诺单位范围连续且尾块补齐全部单位、目标降低仅退役未开始实例且旧计划版本仍可读、schedule_group 任一部分放不下时整组不排且跨度≤2天、priority_class 任务先占容量（诊断占首个可容纳周末、论文素材在 start_date+6 前排完）、fixed_weekday 复盘只排周日、可拆任务任何非最终块至少承诺一个单位且分钟按 b(n) 与规划游标守恒（est=61、2单位拆两块为31+30，不膨胀）、模拟组窗口内未完成成员时组尝试在读取今日/生成预览/提交日志事务中惰性结算为 failed、重排创建新组尝试并为全部成员生成新任务实例（旧实例与日志保留为失败证据，计入投入与弱项但不计入模拟与全文目标）、未开始组尝试被重排移动时转 cancelled、成功组尝试成员日志被编辑为不合格或删除时组状态回退且模拟目标撤回、组成员日志凭 schedule_group_attempt_id 绑定同一尝试（跨设备一致）、组失败重试后模拟论文修改不依赖旧实例（新组尝试论文合格即可排入修改并达成 essay-simulation）、组成员开始计时/产生草稿/提交非草稿日志均使组进入 active、窗口外或组终态开始计时返回 422 GROUP_ATTEMPT_CLOSED、stop 草稿继承组尝试标识、含重试新成员的预览不产生任务行且放弃预览无残留、应用预览先建任务实例与成员行再落 plan_items、同幂等键复用同一批预生成 task_id、成员多条合格日志按（ended_at、attempt_id）稳定选定且仍有候选时组状态不回退、早期组尝试恢复成功后 planned 重试自动 cancelled、active 重试剩余成员不再计必做缺口、模拟目标按（组完成时间、尝试 ID）唯一计数、模拟目标只接受同一成功组尝试内三条合格最终日志（ended_at 本地日期跨度≤2天）、修改任务完成按 source_attempt_id 隔离（不同来源单位不合并、来源失效后任务重开并重新排程、换绑新有效来源后完成）、成功模拟组的论文及对应修改计入全文目标、failed 组已绑定日志可 PATCH/DELETE 并使组恢复 succeeded、窗口内已绑定草稿在组 failed 后仍可转正式日志、组终态后新补录不得追认绑定、手动补录必须提交 ended_at 且 study_date 由其派生、恢复成功再次失效时窗口内 active 重试恢复必做、过窗口结算 failed 后新建尝试、cancelled 重试不复活、sourced_unit_union 按（确认单位数降序、ended_at 升序、attempt_id 升序）选定唯一规划来源（来源 A 已确认{1,2}、B 已确认{1,3} 时选 A 则承诺第3项、选 B 则承诺第2项，不得按合并集合切片）、同一预览内同一任务来源固定、周完成率按 (task_id,source_attempt_id) 冻结不跨来源合并、修改任务计时与草稿继承计划项来源且提交不得换绑、组过期结算与 422 拒绝同请求时结算与 revision 已持久化、取消接口要求先停活动计时器且 cancelled 不复活、忘记停止修正后的模拟论文不得按 evidence=timer 合格、须转 evidence=manual。

记录测试：同单位重复确认只计一次；修改/删除完成日志撤回done；计时增加不改变完成量；原子事务失败不留半条进度；旧task_revision返回409；qualified_attempt 任务分段确认不判完成（75题分30+45两次确认后任务仍未完成、剩余量不扣减），仅完整连续限时最终日志判完成；evidence=timer 且 requires_single_segment 时暂停恢复后的会话不算合格；evidence=manual 且 manual_attestation_allowed=true 时完整声明的补录可合格，未声明连续时不能合格；每个 attempt_id 只有一个当前最终版本；论文修改日志缺 source_attempt_id、或指向其他实例/不合格/已删除/属于 failed 或 cancelled 组尝试的全文尝试时被拒，来源尝试失效后全文目标达成撤回但修改日志保留；跨午夜修正必须提交 corrected_allocations 且合计等于 corrected_seconds。统计测试：50%/70%/90%三套完整卷平均70%；不足3套不达标；重排移走本周任务不减少承诺分母；视频（数千原速秒单位）与论文（少量检查项）同周时按承诺分钟加权而非单位计数；只核对数量不成立——确认非承诺区间（后900秒替代前900秒）不计完成；跨周任务只计落在本周 target_set 内的确认单位；错误率与掌握输入固定时W精确复现。

计时测试：刷新、暂停/恢复、跨午夜、跨设备接管、双设备并发start只成功一个、超4小时未确认不可提交；stop 后刷新或换设备草稿仍在，可提交或放弃，重复 stop 不产生第二条草稿；忘记停止而修正（corrected_seconds/corrected_allocations）的会话不得作为 evidence=timer 合格证据，须转 evidence=manual 提交实际 ended_at、实际秒数与完整连续限时声明；时间校准使用服务端。并发测试：预览后分别修改预算、日志、复习、计时状态，旧预览全部409；伪造preview归属或分配内容不可应用；同key重试只生成一个版本。

安全和外围测试：匿名及另一账号对表/RPC/上传/签名下载均拒绝；直接PostgREST写不可绕过RPC；RPC关联外键不可跨用户；上传超限/非PDF/确认失败/替换中断/删除重试；下载URL过期拒绝新访问。首账号邀请、密码重置、重复种子初始化、种子升级差异（keep/add/replace/retire）与已完成实例及日志保留、JSON导出字段及无敏感信息、认证邮件与学习提醒分离均须验证。保留375px端到端学习流程验收。

目标与复习测试：提纲目标<全文目标时预览返回422 GOAL_INVARIANT_VIOLATION；降低目标退役任务时其保留依赖闭包内的实例不受影响；模拟论文+修改以 goal_instance_id=essay-simulation 占据全文目标一个名额且普通必做全文数量＝max(0, 全文目标−1)；全文目标=2 时实际安排 1 篇普通必做全文加模拟论文共 2 个全文实例；编辑日志移除 error_id 时未开始复习退役、已开始复习保留并标记来源已撤销；修改 study_date 时仅未开始复习重算到期日。

审阅对照：发现1→6.4—6.6；2→6.3；3→6.1；4→6.2；5→7.1/7.3；6→6.5；7→6.3；8→7.2—7.4；9→6.5；10→6.6/本节；11→文首唯一来源与第五节。v1.2 审阅对照：设置与计划原子应用→2.4/6.3/6.5/7.2；排程结果完整落库（起止时间、sequence、承诺单位）→2.2/3.2/6.5；周完成率分钟加权与基线冻结→6.3；视频回忆拆分为依赖检查任务→6.4；复习项接入统一任务模型→6.4/7.2；阶段目标移出首版范围→一/2.1；种子升级 keep/add/replace/retire→6.4；停止后草稿日志恢复→6.2/7.2；release_offset_days、weekend_only 与 JCS 语义→6.4/6.5。本文定义可实现规则；只有种子数据、RPC和验收实际通过后才能声明应用达到开发基线的实现要求。v1.3 审阅对照：限时任务完成模式→6.1 completion_mode；目标依赖闭包与模拟论文独立→6.3/6.4；模拟组排程与跨度校验→6.4/6.5 schedule_group；特殊任务期限（诊断、论文素材、周日复盘）→6.4/6.5 priority_class、deadline、fixed_weekday；周承诺单位集合与切片起点→6.3/6.5；复习项编辑生命周期→6.1；跨午夜修正归属→6.2。v1.4 审阅对照：默认时段容纳模拟组→6.5 默认时间段与缓冲推导、6.6 模拟组正解样例；模拟论文目标名额闭环→6.3/6.4（goal_index=1、普通必做＝max(0, 全文目标−1)、模拟修改入组）；qualified_attempt 可验证规则→6.1 qualification_rules 与 6.4 各实例参数；schedule_group 确定性搜索→6.5 组排序键与字典序搜索、group_max_span_days=最多两个本地日期；单位边界反推分钟→6.5 及 6.6 单位边界样例。v1.5 审阅对照：模拟组恢复为三项考试任务、修改移出组外→6.4/6.6/五；组尝试失败与重试→6.5 schedule_group_attempt；目标实例身份→6.4 goal_instance_id；手动/计时证据分离→6.1 evidence=timer/manual；分钟守恒→6.5 b(n) 累计边界；黄金样例算术修正→6.6 单位边界与模拟组正解。v1.6 审阅对照：组尝试持久化与成员绑定（表结构、plan_items/study_logs 标识、/today、日志提交、导出、规划快照与 RLS 覆盖）→3.2/3.3/6.1/6.5/7.2/7.4；组尝试状态机、窗口惰性结算、取消与成功可撤回→6.5；剩余分钟唯一公式与预览内规划游标→6.5；论文修改绑定 source_attempt_id→6.1/6.3/6.4；manifest 组字段结构校验→6.4。v1.7 审阅对照：模拟修改依赖按 goal_instance_id 解析、失败组日志不计全文/模拟目标→6.3/6.4/6.5；计时器组绑定、草稿继承与窗口外拒绝→6.2/7.2；预览期 prospective_task_spec 与应用时建实例→6.5；成员多合格尝试稳定选择→6.5；早期尝试恢复成功后的重试处置与唯一计数→6.3/6.5；五个场景测试→第八节。v1.8 审阅对照：修改任务来源隔离完成（sourced_unit_union，来源失效重开重排）→6.1/6.4；来源有效状态更正为排除 failed/cancelled→6.3/6.4；已绑定日志编辑与窗口内草稿转正不受组终态限制、终态后新补录禁追认→6.1/6.5/7.2；ended_at 权威结束时间与 study_date 派生→6.1/6.2/6.3/6.5；恢复成功再次失效的重试恢复规则→6.5。v1.9 审阅对照：sourced_unit_union 规划来源贯穿计划项/游标/周完成率/计时草稿→6.1/6.2/6.3/6.5/7.2；修正计时不得作 timer 证据、转 manual→6.1/6.2；惰性结算与 422 同请求时结算须提交→6.5；组尝试取消命令与 cancelled 单向→6.5/7.2。
