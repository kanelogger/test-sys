# 技能调用 Hooks 与提示词

> 适用基线：`需求方案.md` v2.6（2026-09-07），仅适配 PC。本文是执行约定；hook 表示按项目状态调用技能，只有 `setup-pre-commit` 会创建 Git hook。修订本文不代表已执行下列步骤。

## 一、编排与执行规则

推荐主链：

```text
Step 0 真实 seed JSON + 四场景推演 → 冻结需求
  → Step 1 同一次准备：domain-modeling + codebase-design
  → Step 2 prototype（仅一个明确未决问题）
  → Step 3 PC 关键流程设计：baoyu-design / web-design 二选一
  → Step 4 显式 $to-tickets
  → Step 5 逐票显式 $implement
       ├─ 先记录起始 SHA，再条件调用 tdd
       ├─ 最小脚手架后，一次 setup-pre-commit 独立变更
       ├─ 验证 → 提交 → code-review → 修复提交与复核
       └─ 真实失败时调用 diagnosing-bugs
  → Step 6 全量 code-review + 真实运行验收
```

1. `需求方案.md` 是产品行为唯一基线；`CONTEXT.md` 记录领域语言，接口契约与设计均服从需求。未决项先记录，不擅自修改需求。
2. Codex 使用 `$to-tickets`、`$implement` 显式引用。本地两技能的 `agents/openai.yaml` 均设置 `allow_implicit_invocation: false`；`SKILL.md` 的 `disable-model-invocation` 属于宿主相关元数据，不能据此假定所有宿主都支持 `/技能名`。见 [Codex 技能文档](https://learn.chatgpt.com/docs/build-skills)。执行时仍须核实技能已安装并可调用。
3. 每张 ready ticket 只有一个 `implement` 实施入口；TDD 在它内部执行，不能先单独实现一遍再调用 implement。
4. 先确认首个闭环需要的领域、接口和 PC 设计；其他页面与接口随票补齐，不等待五页全部高保真后才开工。
5. `prototype`、`tdd`、`diagnosing-bugs` 按条件调用；已确认且未变化的产物不重做。设计二选一，不生成竞争方案。
6. 票级 Spec 只检查本票验收和全局不变量；尚未解锁的后续票不算本票缺漏。最终评审才覆盖全部需求。
7. 放行依据是可观察行为及真实证据，构建成功不能代替 UI、持久化、事务或部署验收。

### 执行前的持久状态约定

本矩阵不会自动触发技能。开始执行本流程前，在项目 `AGENTS.md` 引用本文，并建立**一个**工作流状态文件，例如 `docs/agents/workflow-state.md`；此次文档修订不创建这些文件。

状态文件保存：需求版本和冻结状态、当前票及 blockers、seed/推演/设计产物路径、简短接口契约或既有契约路径、项目 baseline SHA、当前票 start SHA、已验证与未验证项、评审覆盖的 HEAD、待决问题。恢复上下文时先读它；有产物则核实后续用，无变化不重复调用技能。

短 TypeScript interface 草案可放在状态文件的契约区；已有设计记录则引用它。票据只引用契约并描述行为约定，避免复制实现代码。技能缺失、tracker 未配置或前置条件未满足时，应明确记录阻塞，不伪造已完成状态。

## 二、技能职责与状态 Hook

| Hook            | 前置条件                                          | 调用 / 职责                                       | 完成标志                                                    |
| --------------- | ------------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------------- |
| H-00 冻结准备   | 尚无真实 seed 或四场景推演                        | 主 agent 产出与校验，无需新技能                   | 真实 JSON、逐日校验和推演证据齐全，需求冻结                 |
| H-01 领域与接口 | H-00 完成                                         | 同次准备使用 `domain-modeling`、`codebase-design` | 简短词汇表、首个闭环接口/错误/事务边界和测试 seam 已确认    |
| H-02 单点试验   | 一个明确问题无法靠现有证据判断                    | `prototype` LOGIC 或对应分支                      | 有界示例得出结论，写回契约；原型可丢弃                      |
| H-03 PC 设计    | 关键语义稳定                                      | 推荐 `baoyu-design`；`web-design` 为替代          | PC 导航及“今日→记录→完成”已确认，其余状态有后续归属         |
| H-04 拆票       | H-01、H-03 完成；触发的 H-02 已解决；tracker 可用 | 显式 `$to-tickets`                                | 用户确认粒度和依赖后发布；全部验收有票据归属                |
| H-05 逐票实施   | 所有 blockers 完成                                | 显式 `$implement`，先保存 SHA                     | 本票验证通过，最终 HEAD 已评审，状态已更新                  |
| H-05a TDD       | 本票触及关键数据行为                              | `tdd`，在已确认 seam 上 red → green               | 捕获真实业务回归的测试及失败/通过证据                       |
| H-05b 提交门禁  | 最小脚手架可运行，前置齐全，尚无有效 hook         | `setup-pre-commit`，一次独立变更                  | 真实提交触发格式化、typecheck、test 并通过                  |
| H-05c 票级评审  | 当前票已提交，start SHA 已保存                    | `code-review <start-sha>`                         | 本票 Spec 缺口与硬性 Standards 违规已处理，最终 HEAD 已复核 |
| H-06 缺陷诊断   | 测试、浏览器或构建有可观察失败                    | `diagnosing-bugs`                                 | 原始复现通过、回归通过、诊断代码清理                        |
| H-07 总体验收   | 全部票完成                                        | `code-review <baseline-sha>` + 真实应用           | 全部需求与第七节验收均有证据                                |

`domain-modeling` 只在术语变化时更新；`codebase-design` 只在接口/事务边界变化时更新；设计只细化受影响流程；票据已发布则只修订受影响票；有效 pre-commit 不重复安装；诊断只响应真实失败。

## 三、逐步提示词与交付物

### Step 0. 产出真实 seed，完成冻结前推演

**调用：** 主 agent 直接完成。先核实是否已有符合 v2.6 的产物，避免重复生成。

```text
读取 ./需求方案.md v2.6、./系统分析师备考总计划.md、./备考资料.md。
产出随应用分发的真实初始计划 JSON，并记录路径与 seedVersion；不要只做样例。

顶层按需求包含 seedVersion、settings、resources、planItems，不含 StudyLog。
声明覆盖 2026-09-08 至 2026-10-23，并逐日安排；覆盖起止字段名若尚未定义，
先在工作流状态文件记录表示约定及待确认点，不擅自改需求或声称已冻结 schema。
每项 source=seed、status=pending、lineageId=id，预计分钟为正整数，完成标准非空。
校验 ID/引用/资源规则和逐日覆盖；周一至周四每天≤90分钟、周五≤60、周六日≤240。
种子星期预算与 Settings.defaultDailyMinutes 的通用 UI 参考线分开，不自动按星期改设置。
按已报名、已缴费、零进度排布；PDF 只写文件名，不加入内容或本机绝对路径。

使用这份 JSON 推演四场景：当天完成、逾期移动、忘记登记后补录、备份恢复。
逐场景列出输入、操作、前后实体、引用与不变量检查、界面反馈。
补记已学习的历史任务应完成原 pending，日志留在计划日；当天补做则先移动至执行日。
备份恢复推演应包含完整替换和失败零修改。区分模型推演与实现后的运行验收：
这里不能证明真实 IndexedDB 事务、并发或恢复已通过。报告待决项，解决后记录需求冻结。
```

**交付与放行：** 真实 seed JSON、日期/预算/字段与引用校验结果、四场景推演记录；阻塞语义已解决并记录冻结状态。后续票负责集成这份 seed，不另造一份内容基线。

### Step 1. 同次准备领域语言与首个闭环接口

**调用：** 先 `domain-modeling`，随后在同一任务中使用 `codebase-design`；保持两类产物职责清晰。

```text
使用 domain-modeling 读取 ./需求方案.md、Step 0 的 seed 与推演，建立或更新简短 CONTEXT.md。
定义 Resource、PlanItem、StudyLog、AppMeta、Settings、lineage、根/前驱/后继、
pending/completed/moved/skipped、seed/manual/backfill、补录、初始化与完整替换恢复。
只记录领域语言和关系，不写 React、IndexedDB 表结构或函数名。
明确每个 lineage 内最多一个 pending，不同 lineage 可以同时存在 pending。
补记保留原计划日；补做先移动至执行日；StudyLog.date 必须等于关联 PlanItem.date。
只有难以逆转、缺少背景令人意外且存在真实取舍的决策才创建 ADR。

随后使用 codebase-design，先定义“初始化→今日→记录→完成”所需的公开 command/query，
给出简短 TypeScript interface、前置条件、结果/错误、事务边界及 TDD seam。
模块隐藏 IndexedDB 与校验细节，UI 不拼装事务；不为假想第二种存储引入 adapter。
涉及补录时，优先完成已有 pending，只填写日志字段；无对应任务才原子创建 backfill
PlanItem 与 StudyLog，收集预计分钟和完成标准，并防止同一次提交重复创建。
完成、移动、跳过、删除在写事务内重读当前状态，过期操作放弃并提示；不做标签页自动同步。
提前标明导出须覆盖全部相关表的同一只读事务快照，导入须完整校验后原子替换。
后续移动链、日志编辑、备份接口随相关票细化；实施该票前确认其契约与 seam。
将简短契约保存在工作流状态文件或既有设计记录，票只引用并写可观察行为。
```

**交付与放行：** 简短 `CONTEXT.md`、首个闭环契约及测试 seam；相关语义没有未决冲突。后续接口尚未细化不阻塞首个闭环。

### Step 2. 仅在存在明确未决问题时制作原型

**Hook：** 必须能写出一个待回答的问题和决定后续方案的标准；没有则跳过。

```text
调用 prototype 的 LOGIC 分支。唯一问题：<明确未决问题>。
例如“连续移动后的末端跳过，是否能让用户清楚理解原计划与最终状态”。
以需求与已确认契约为约束，制作可双击的单 HTML 内存原型，不接生产 IndexedDB。
只提供回答该问题必需的操作与 walkthrough，显示操作前后状态及相关不变量。
移动链示例可覆盖连续移动、末端删除拒绝、跳过、完成与重复请求；检查每个 lineage
最多一个 pending，不把不同 lineage 的 pending 合并计数。
输出本次示例支持的结论、尚未覆盖的情形及契约需要的修订。
有限 walkthrough 不能证明任意合法序列均正确，真实事务和并发留给生产测试。
原型保存在 throwaway branch，不进入主分支；仅结论进入正式契约及票据。
```

**交付与放行：** 可丢弃原型、可复查的示例结果、该单点问题的决定；不得用它替代正式 UI 设计或生产事务测试。

### Step 3. 确认 PC 导航与关键流程

**分支 A：推荐 `baoyu-design`。** 先确认信息结构和首个闭环，其余页面按票细化。

```text
调用 baoyu-design。以 ./需求方案.md、./CONTEXT.md 和真实 seed 为依据，为单人学习工具
制作自包含交互原型，保存到 ./designs/study-assistant/。采用紧凑工作型界面、真实中文
内容密度和 lucide 图标，不引入营销 hero、装饰性大卡片或夸张滚动叙事。

先按技能要求确认上下文与方向，完成 PC 侧栏的今日、计划、记录、历史、资源导航，
重点确认“今日→填写实际分钟与总结→完成”、逾期待处理、完成标准和资源入口。
今日预算 X 仅计今天 pending+completed 的 plannedMinutes；排除 moved、skipped 和
未移入今天的逾期项；Y 为设置的通用参考值。展示考试剩余天数与超预算提示。
补录先选历史 pending，只填日志；无对应任务才显示新建补录字段。
明确提交中、重复点击、旧标签页过期操作的反馈，以及补录与补做的日期区别。

计划增删排序/移动/跳过/设置、日志编辑、历史状态、资源复制和备份导入确认/失败态
先列待拆票的状态清单，Step 4 分配票据归属，再在各票实施前细化；导出交互遵守一致快照契约。
在常见 PC 宽度实际预览首个闭环，检查可读性、溢出、遮挡、键盘与鼠标操作，保存视觉证据。
```

**分支 B：需要 `DESIGN.md` 优先流程时改用 `web-design`，不与 A 串行。**

```text
调用 web-design，在 ./designs/study-assistant-design-spec/ 执行 DESIGN.md-first 流程。
事实来源为需求、领域词汇、真实 seed 和已确认契约；产品是仅 PC 的五页面操作型工具。
规范先覆盖 PC 导航、今日与记录完成闭环及关键反馈，采用克制 L1 交互；无营销叙事。
确认规范后在独立设计目录制作可运行参考，使用 fixture，不接生产 IndexedDB。
补录优先已有 pending、重复/过期提交、导出快照及后续状态清单均遵守分支 A 的要求。
完成规范合规审计与 PC 视觉/交互验证；其余页面随票补充规范和参考实现。
```

**交付与放行：** 一个已确认的设计分支、PC 导航和首个闭环、视觉证据、后续状态清单。路径写入工作流状态；不要求五页全部高保真才能拆票。

### Step 4. 配置 tracker 并拆成纵向票

先读取已有 tracker 约定。未配置时，先核实 `setup-matt-pocock-skills` 是否已安装且可调用；有则显式调用并选择本地 Markdown tracker。缺失时明确说明，按 `to-tickets` 的本地文件契约手工配置目录、模板、状态和依赖规则，写入 `docs/agents/issue-tracker.md`；不得假装 `/setup-matt-pocock-skills` 必然可执行。完成配置后再拆票。

```text
$to-tickets ./需求方案.md
读取 CONTEXT、工作流状态中的 seed/契约/设计引用与 tracker 约定。
按“今日与记录闭环→计划增删排序→历史与资源→导出导入与部署”拆为可独立演示的纵向票。
每票包含完成该行为必要的数据、UI 和验证，只依赖真正 blockers，适合一个新上下文。
允许一张最小脚手架准备票作为纵向规则的前置例外；质量门禁是其后的一次独立变更。
不得把业务数据层整体拆为横向大票，也不要求所有后续接口预先设计完。

验收映射须覆盖：既有真实 seed 集成与初始化原子性、今日预算/设置、完成、移动 lineage、
删除/跳过/逾期、补录优先已有 pending、无对应任务的新建补录及防重复、日志编辑、
写事务重读与旧标签页、计划增删排序、历史/资源、导出一致快照、备份字段与兼容版本、
校验失败零修改、确认后原子完整替换、GitHub Pages 子路径和最终运行验收。
票据引用已有契约与设计，写行为验收，不复制 TypeScript 实现片段。
先展示票据、验收映射与 blocking edges，用户确认后发布到已配置 tracker。
总计划和备考资料只作 seed 内容来源；不引入需求排除的功能。
```

**交付与放行：** 本地 tracker 约定、一票一文件、blocking edges、全部需求验收到票据的映射。设计细化和接口确认列为对应票的实施前置。

### Step 5. 单入口逐票实施、验证与评审

**调用：** 每张 ready ticket 显式 `$implement`。首次实施前保存项目 baseline SHA；每票任何编辑及 TDD 前保存 start SHA。仓库尚无提交时先建立可引用基线，不使用不存在的 SHA。

```text
$implement <ready-ticket-ref>
读取该票、需求 v2.6、CONTEXT 和工作流状态引用的 seed/契约/设计；核实所有 blockers 已完成。
开始任何编辑或 TDD 前保存当前 commit SHA。只实现本票；缺少本票契约或设计则先补齐确认。
命中下述 TDD 条件时在 implement 内逐个 red→green，不另开一次先行实现。
UI 在真实 PC 应用中验证，修复本票引起的测试与类型错误，运行聚焦验证和本票所需完整检查。

本项目明确覆盖 implement 原“先评审再提交”的默认顺序：完成验证后先提交，
再以本票 start SHA 调用 code-review。原因是本地 code-review 读取固定点到 HEAD 的提交差异。
Spec=本票验收+需求的全局不变量；未解锁的后续功能不记作本票缺漏。
修复 Spec 缺口与硬性 Standards 违规后再次验证、提交，并以同一 start SHA 复核。
只有最终 HEAD 被审过且通过，才标记票完成，并记录 SHA、实际验证和剩余事项。
```

**最小脚手架准备票：** 建立 React + Vite + TypeScript、HashRouter、必要测试工具与 typecheck/test/build 脚本；按真实仓库信息设置 Vite base，开发服务器能打开。它也走上述 SHA、提交和票级评审流程，不提前实现后续业务。

**提交门禁只安装一次：** 必须已有 `package.json`、锁文件、可工作的 typecheck/test 脚本及 Git 仓库；已有有效 hook 则跳过。

```text
调用 setup-pre-commit，按锁文件识别包管理器，配置 Husky、lint-staged、Prettier。
遵守现有格式约定，pre-commit 执行 staged 格式化、完整 typecheck 和 test。
只改门禁配置，不混入功能；验证 hook 可执行、prepare 正确，实际运行所有门禁。
单独提交并确认真实提交触发门禁；记录结果，将这次变更纳入固定 SHA 评审。
```

**TDD 触发范围：** 初始化及标记同事务；完成与唯一日志；移动链/跳过/删除限制；补录两条路径及同次提交防重复；写事务重读、重复点击与旧标签页；日志编辑/日期不变量；本地日历日及夏令时；导出一致快照；导入校验、失败零修改与原子完整替换。

```text
在当前 implement 内调用 tdd，目标为 <本票可观察行为>，使用本票已确认的公开数据模块 seam。
从需求取得独立期望，一次一个纵向 red→green：运行失败，再做最小实现并验证通过。
检查 command 结果、持久化 query、事务失败后的原状态和领域不变量。
需要改 seam 时先更新并确认契约；不测试私有实现、mock 回显或仅断言不抛错。
并发/回滚/快照必须验证真实 IndexedDB 的事务语义，内存原型和纯 mock 不能充当通过证据。
```

**真实失败才诊断：**

```text
调用 diagnosing-bugs。失败场景：<输入、操作、期望、实际与现有证据>。
建立可重复反馈环，定位根因，做最小修复，重跑原始复现与受影响回归，清理诊断代码。
修复纳入当前票的提交和复核；不得绕过失败测试或把未执行项标为通过。
```

**交付与放行：** 本票代码、实际验证证据、提交、最终 HEAD 的两轴评审；工作流状态可支持新上下文继续。

### Step 6. 全量评审与真实运行验收

```text
以 <project-baseline-sha> 调用 code-review，Spec 使用完整 ./需求方案.md v2.6。
执行 Standards 与 Spec 两轴总评审，修复全部 Spec 缺口、错误实现、范围膨胀和硬性违规。
修复后验证、提交，再以同一 fixed point 复核，确保最终 HEAD 已审过。

构建生产版本，使用真实应用和浏览器能力逐项验收全部需求及第七节，至少覆盖：
1. 真实 seed 随包分发，范围/逐日预算/完成标准/资源合法；首开按当天显示任务或空态，
   过去 pending 进入逾期；初始化失败回滚，删空刷新不重灌，新 seed 版本不覆盖已有数据。
2. 默认今日、PC 侧栏；考试日期可改并影响剩余天数；预算 X/Y 及排除项口径正确，
   Y 不自动按星期切换，设置不自动修改任务。完成必须填正整数分钟与非空总结。
3. 完成与唯一日志原子持久化，失败无孤立完成状态；刷新后完整保留。
4. 补录已有 pending 不增任务且解除逾期，日志留计划日；无对应任务才建 backfill，
   预计分钟/完成标准必填且任务日志同事务；补做先移动到执行日，日志编辑不改任务终态。
5. 计划可增删排序；移动仅 pending、同日期无修改、连续移动引用正确且每个 lineage
   最多一个 pending；各 lineage 可并存。链无分叉/环/悬空，末端拒绝硬删，终态不可硬删。
6. 逾期可移今天、移明天或跳过；本地日历“明天”正确；历史显示绝对目标日期、
   计划与实际、moved/skipped/backfill。跳过不删除链节点。
7. 重复点击完成/移动/新建补录不重复写入；旧标签页过期操作重读校验并提示；
   还要覆盖跳过/删除的状态重读。首版无自动标签页同步。
8. 网页资源正常跳转；本地 PDF 仅显示并复制文件名，不打开/上传/保存绝对路径。
9. 导出字段完整且来自同一只读事务；并发完成/移动/补录期间的导出仍可通过导入校验。
10. 非法 JSON、版本/字段/ID/引用/日期/日志/lineage 违例均拒绝且原数据逐项不变；
    initializedSeedVersion 非空且属于 supportedSeedVersions，兼容历史版本可恢复，
    不要求等于当前随包版本；合法备份经确认原子完整替换全部数据含 AppMeta。
11. 导出→清站点数据→导入内容一致，恢复后刷新仍完整；恢复事务失败保留原数据。
12. GitHub Pages base+HashRouter 下所有页面直接刷新正常，PC 关键流程无溢出或遮挡；
    浏览器网络记录无上传计划、总结、PDF 信息的请求，未引入首版排除的能力。

为每项报告执行方式、观察结果与证据路径；未执行不得写成通过。失败调用 diagnosing-bugs，
修复后重跑受影响项和完整关键闭环。冻结前推演、构建和测试数量都不能代替运行验收。
```

**最终交付与放行：** 全量两轴评审、生产构建、逐项验收证据、可部署静态产物；所有验收有真实通过证据，最终 HEAD 已评审，无未解决 Spec finding，主分支无 throwaway 原型、fixture 持久化或临时诊断代码。
