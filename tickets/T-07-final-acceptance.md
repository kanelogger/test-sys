# T-07 全量评审与最终运行验收

- 状态：pending
- Blocked by：T-06
- 契约与设计引用：`需求方案.md` v2.6 全文（Spec 基线）；`draft.md` Step 6；项目 baseline SHA `f600aaa3bb5328cf016a3b34296d9b977fc2031b`；各票最终 SHA 与已记录证据。

## 范围

- 以项目 baseline SHA 调用 code-review，执行 Standards + Spec 两轴总评审；修复全部 Spec 缺口、错误实现、范围膨胀与硬性违规，复核至最终 HEAD 通过。
- 生产构建，使用真实应用与浏览器能力逐项运行验收。

## 非目标

- 不新增首版范围外功能；不以构建成功、测试数量或冻结前模型推演代替运行验收。

## 行为验收（`draft.md` Step 6 十二项，逐项记录执行方式、观察结果与证据路径）

1. 真实 seed 随包分发，范围/逐日预算/完成标准/资源合法；首开按当天显示任务或空态，过去 pending 进入逾期；初始化失败回滚；删空刷新不重灌；新 seed 版本不覆盖已有数据。
2. 默认今日、PC 侧栏；考试日期可改并影响剩余天数；预算 X/Y 及排除项口径正确，Y 不自动按星期切换，设置不自动修改任务；完成必须填正整数分钟与非空总结。
3. 完成与唯一日志原子持久化，失败无孤立完成状态；刷新后完整保留。
4. 补录已有 pending 不增任务且解除逾期、日志留计划日；无对应任务才建 backfill（预计分钟/完成标准必填、任务与日志同事务）；补做先移动到执行日再完成、日志记执行日；日志编辑不改任务终态。
5. 计划可增删排序；移动仅 pending、同日期无修改、连续移动引用正确且每个 lineage 最多一个 pending、各 lineage 可并存；链无分叉/环/悬空，末端拒绝硬删，终态不可硬删。
6. 逾期可移今天、移明天或跳过；本地日历「明天」正确；历史显示绝对目标日期、计划与实际、moved/skipped/backfill；跳过不删除链节点。
7. 重复点击完成/移动/新建补录不重复写入；旧标签页过期操作重读校验并提示；覆盖跳过/删除的状态重读；首版无自动标签页同步。
8. 网页资源正常跳转；本地 PDF 仅显示并复制文件名，不打开/上传/保存绝对路径。
9. 导出字段完整且来自同一只读事务；并发完成/移动/补录期间的导出仍可通过导入校验。
10. 非法 JSON、版本/字段/ID/引用/日期/日志/lineage 违例均拒绝且原数据逐项不变；initializedSeedVersion 非空且属于 supportedSeedVersions，兼容历史版本可恢复，不要求等于当前随包版本；合法备份经确认原子完整替换全部数据含 AppMeta。
11. 导出→清站点数据→导入内容一致，恢复后刷新仍完整；恢复事务失败保留原数据。
12. GitHub Pages base+HashRouter 下所有页面直接刷新正常；PC 关键流程无溢出或遮挡；浏览器网络记录没有任何上传计划、总结或 PDF 信息的请求；未引入首版排除的能力（后端/登录、自动排程、计时器、统计复盘、文件上传、提醒推送）。

## 验证要求

- 每项给出执行方式、观察结果与证据路径；未执行不得写成通过。失败调用 diagnosing-bugs，修复后重跑受影响项和完整关键闭环。
- 放行时主分支无 throwaway 原型、fixture 持久化或临时诊断代码；最终 HEAD 已评审，无未解决 Spec finding。

## 实施前置确认

- 全部前置票为 done 且各自最终 HEAD 已评审；需求冻结状态与 COV-01/FMT-01 决定已记录于 `docs/agents/workflow-state.md`。
