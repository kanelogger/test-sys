# 系统分析师学习助手

单人备考中的计划、学习记录与资源。领域语言依据 `需求方案.md` v2.6；真实初始计划及 Step 0 推演用于核对含义，不代表用户已有学习进度。

## Language

### 实体

**Resource（资源）**：
学习任务的资料入口，可供多个 PlanItem 引用，每个 PlanItem 至多关联一个资源。网页资源是 http(s) 地址；本地 PDF 资源只是文件名，不代表文件内容或本机路径。

**PlanItem（计划项）**：
某个本地日历日的一次可完成学习时段，包含科目、标题、预计分钟、完成标准、可选资源及日内次序。预计分钟是正整数，完成标准非空；同一学习任务改期前后的各次安排是同一 lineage 中不同的 PlanItem。
_Avoid_：用“任务”混指单个计划项和整条移动链。

**StudyLog（学习日志）**：
一个 completed PlanItem 的实际学习记录，包含正整数实际分钟、去除首尾空白后非空的总结及可选成绩文本；每个 completed 项恰有一条日志，其他状态没有日志。`StudyLog.date` 必须等于关联 `PlanItem.date`，表示实际学习日而非登记日；日志内容修正不改变任务终态，不删除完成日志。

**AppMeta（初始化标记）**：
当前学习数据所沿用的初始计划版本；`initializedSeedVersion` 为空表示尚未初始化，非空表示已初始化。它是初始化与否的唯一依据，不以剩余计划项数量判断，也不表示学习完成进度。

**Settings（设置）**：
考试日期与默认每日分钟这两个显示参考：前者决定考试剩余天数，后者是今日预算的通用参考线。它们不自动调整计划，默认每日分钟也不是初始计划按星期区分的内容预算。

### 移动关系

**lineage（移动链）**：
同一学习任务历次日期安排构成的无分叉、无环、无悬空引用的链，`lineageId` 等于根的 id，所有节点都能从根到达。每个 lineage 内最多一个 pending，不同 lineage 可以同时存在 pending；末端 completed 或 skipped 时，该 lineage 没有 pending。

**根**：
一个 lineage 唯一没有前驱的 PlanItem。seed、manual、backfill 新建项都是根，`lineageId = id`；只有移动产生的后继继承已有 `lineageId`。

**前驱**：
通过 `movedToPlanItemId` 指向当前项的 moved PlanItem。除根外，每项恰有一个前驱。

**后继**：
改期后承接同一学习任务的新 PlanItem，与前驱属于同一 lineage。一个 PlanItem 当且仅当状态为 moved 才有一个后继，其他状态没有后继。

### 状态与来源

**pending（待处理）**：
尚未完成、移动或跳过的计划项，无学习日志。只有未产生记录且没有前驱引用的 pending 可永久删除；移动链末端的 pending 只能完成、继续移动或跳过。

**completed（已完成）**：
已经登记实际分钟和总结、且恰有一条学习日志的终态，无后继。它不因打开资源或预计学习时间已过而自动成立。

**moved（已移动）**：
保留在原计划日的改期历史项，无日志且恰有一个后继，目标是后继的绝对日期。只有 pending 可以移动；目标与原日期相同不构成移动。

**skipped（已跳过）**：
决定不再执行但保留历史的终态，无日志、无后继。completed、moved、skipped 均不永久删除，其日期、状态和移动关系不通过普通编辑改变。

**seed（初始计划来源）**：
来自随应用分发的真实初始计划，初始均为独立 pending 根。来源不是状态，完成或移动不会把 seed 改成其他来源。

**manual（手动计划来源）**：
用户自行安排并新建的计划根，而非初始计划或事后补建的历史计划。移动产生的后继沿用原项来源。

**backfill（补建历史计划来源）**：
补录时系统没有对应任务，才补建并立即完成的历史计划根。它只代表新建补录项，不用于标记原有 seed/manual 任务的事后登记。

### 日期、补录与数据生命周期

**本地日历日**：
学习者本地时区的年月日，写作 YYYY-MM-DD；“明天”是操作当天的本地日历次日。计划日、实际学习日与登记日含义不同，不能以登记日替代实际学习日。

**逾期待处理**：
日期早于今天且仍为 pending 的计划项，不是另一种 status。它不会自动改期，需按事实补记、移动后补做或跳过。

**补录**：
事后登记历史学习：优先完成对应历史日的已有 pending，只填写日志；系统无对应任务时才补建 backfill PlanItem 及其 StudyLog。补建仅限当时确有计划且记得预计分钟的学习，需如实提供预计分钟和完成标准，不用实际分钟冒充预计分钟，也不记录临时未计划学习。

**补记**：
原计划日已经学习、后来才登记的补录，完成原 pending，保留原计划日与来源，日志也留在原计划日。它不增加计划项，不把登记日变成学习日。
_Avoid_：补做。

**补做**：
原计划日没有学习、延后到执行日才学习；先移动到执行日，再完成后继，日志日期为执行日。它不同于在原计划日已经学过的补记。
_Avoid_：补记。

**初始化**：
首次采用随包初始计划，同时形成对应 AppMeta 标记、Settings、Resource 与 pending PlanItem，不产生学习日志。初始化后即使计划删空，或随包初始计划更新，也不重新灌入或覆盖已有学习数据。

**完整替换恢复**：
以经完整校验并确认的兼容备份整体取代当前 AppMeta、Settings、Resource、PlanItem 与 StudyLog，本地独有内容不保留，失败或取消则原数据不变。它不是合并或初始化，恢复的初始计划版本可以是仍兼容的历史版本，不必等于当前随包版本。
_Avoid_：合并导入、用备份初始化。
